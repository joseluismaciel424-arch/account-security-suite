# Local development only — never commit real values.
PORT=4000
JWT_SECRET=generate_a_long_random_value_locally
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/account_security
REDIS_URL=redis://localhost:6379
